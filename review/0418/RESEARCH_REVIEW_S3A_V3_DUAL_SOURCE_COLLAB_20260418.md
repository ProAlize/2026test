# S3A v3 双源协同调查结论（2026-04-18）

## 1. 调查目标
- 确认本次 `v3` 训练在完成后，是否实现了“**双源协同**”（不是只看进程是否结束）。
- 结论判据按项目既有标准：重点看 `step >= 25000`（self warmup 结束后）的 `dual_alive`、`a_dino/a_self`、`synergy_margin`、`loss_self_only`。

## 2. 数据来源（已归档于本目录）
- `launch_v3_45k_20260417_171937.log`
- `metrics_v3_45k_20260417_171959.jsonl`
- `resolved_args_v3_45k_20260417_171959.json`
- `s3a_v3_45k_maintainer.log`

## 3. 训练是否正常结束
- 训练达到 `max_steps=45000` 并写出最终 checkpoint：`launch` 第 281 行。
- 训练打印 `Done!`：`launch` 第 282 行。
- 结论：训练流程正常结束，非异常中断。

## 4. 双源协同结论（核心）
## 4.1 结论一句话
- **未实现“正向双源协同”**（`synergy_margin` 在后期持续为负），但**实现了双源共存/共激活**（`dual_alive` 和 `a_dino/a_self` 指标显示两源同时在用）。

## 4.2 量化统计（仅 `step >= 25000`）
- 统计区间：`step 25000 -> 45000`，共 `41` 个记录点（全程共 `90` 个 metric 点）。
- `dual_alive_ratio = 95.12%`。
- `a_dino > 0.1 且 a_self > 0.1` 比例 `95.12%`。
- `synergy_margin_mean = -0.1398`，`synergy_margin > 0` 比例 `0%`。
- `loss_self_only_mean = 0.06337`，落在 `[0.3, 1.0]` 的比例 `0%`。

## 4.3 代表性样本点

| step | a_dino | a_self | dual_alive | synergy_margin | LfusedProbe | Ldino | Lself |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 25000 | 1.000 | 0.000 | 0 | -0.4492 | 0.5438 | 0.5438 | 0.0946 |
| 30000 | 0.287 | 0.712 | 1 | -0.1252 | 0.1823 | 0.5530 | 0.0571 |
| 35000 | 0.287 | 0.712 | 1 | -0.1233 | 0.1829 | 0.5456 | 0.0596 |
| 40000 | 0.287 | 0.712 | 1 | -0.1218 | 0.1877 | 0.5441 | 0.0659 |
| 45000 | 0.287 | 0.712 | 1 | -0.1196 | 0.1896 | 0.5391 | 0.0700 |

## 5. 关键日志证据（行号）
- `self warmup` 结束并执行 router reset：`launch` 第 230 行（`[step=25000] Router output layer reset ...`）。
- 双源稳定激活样本（`dual_alive=1`）：`launch` 第 244 行（`step=30000`）。
- 最后训练点（`step=45000`）：`launch` 第 279 行。
- 正常收尾：`launch` 第 281-282 行（`Reached max_steps=45000` + `Done!`）。
- 维护日志后续 `PROCESS_LOST`（如 `maintainer` 第 601 行）对应的是训练完成后进程退出，不是崩溃。

## 6. 与“成功标准”的对照判定
- `dual_alive > 50%`：满足。
- `a_dino` 与 `a_self` 同时显著非零：满足（后期长期 `0.287 / 0.712`）。
- `synergy_margin > 0`：不满足（全后期为负）。
- `loss_self_only ≈ 0.3~1.0`：不满足（均值约 `0.063`）。

最终判定：**双源被激活，但没有形成正向协同增益，判为“协同失败/部分达成（仅共存）”。**

## 7. 备注
- 以上统计仅基于本次 run 的归档日志和指标文件；未混入其他 run。
- 复算方式：读取 `metrics_v3_45k_20260417_171959.jsonl` 的 `record_type=metric` 行，对 `step>=25000` 子集做比例与均值统计。
